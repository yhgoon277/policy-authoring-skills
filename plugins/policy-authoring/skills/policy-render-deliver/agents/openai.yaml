interface:
  display_name: "Policy Render & Deliver"
  short_description: "배포물을 기본 원본 보존(R1 WAIVED) 또는 --golden 옵트인 splice로 빌드하고 5원칙 수용 게이트를 실행합니다"
  default_prompt: "Use $policy-render-deliver to build the source-preserved deliverable (default) or splice the golden-grade deliverable HTML with --golden."
policy:
  allow_implicit_invocation: true
