interface:
  display_name: "Policy Render & Deliver"
  short_description: "spec을 6섹션 self-contained 미리보기로 렌더하고 NC 변환본에 배포본 HTML을 splice합니다"
  default_prompt: "Use $policy-render-deliver to render the 6-section preview and splice the golden-grade deliverable HTML."
policy:
  allow_implicit_invocation: true
